// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://jackcmiller4.github.io',
  base: '/bosker-excavating',
  output: 'static',
});
